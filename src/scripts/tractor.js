export const plantSeeds = (plantingPlanArray, plantSeedFunctionObject) => {
    for (const rows of plantingPlanArray) {
        for (const plant in rows) {
            switch plant {
                
            }
            const genericSeed = createAspar

addPlant(asparagusSeed)
        }
    }
}